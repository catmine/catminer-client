%h1= @rig.name
%h3 Rig Status
%table.table.table-responsive.table-striped
  %thead
    %tr
      %th{ scope: 'col' } Value
      %th{ scope: 'col' } Parameter
  %tbody
    %tr
      %td First Seen
      %td= l @rig.created_at, format: :long
    %tr
      %td Current mining command
      %td= @mining.to_s
    %tr
      %td GPUs
      %td= @rig.gpus.count
    %tr
      %td AV Utilization (%)
      %td= @rig.utilization
    %tr
      %td Power (W)
      %td= @rig.power
    %tr
      %td AV Temp (C)
      %td= @rig.temperature
%hr/
%h3 GPUs
%table.table.table-responsive.table-striped
  %thead
    %tr
      %th{ scope: 'col' } No
      %th{ scope: 'col' } GPU
      %th{ scope: 'col' } Utilization (%)
      %th{ scope: 'col' } Power (W)
      %th{ scope: 'col' } Temp (C)
  %tbody
    - i = 1
    - @rig.gpus.enable.each do |gpu|
      %tr
        %td= i
        %td= gpu[:name]
        %td= gpu[:utilization]
        %td= gpu[:power]
        %td= gpu[:temperature]
      - i += 1
